import React from 'react'
import Card from '@components/Card'

function ResetPassword({ color }) {
	return (
		 <Card
		 	title="Reset password"
		 	style={{color}}
		 >
		 	Test
		 </Card>
	)
}

export default ResetPassword
